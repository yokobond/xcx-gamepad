{
  "name": "xcx-gamepad",
  "version": "0.1.1",
  "description": "gamepad in Xcratch",
  "scripts": {
    "register": "npx xcratch-register --link -C --id=webGamepad --gui=\"../scratch-gui\"",
    "build": "npx xcratch-build --module=webGamepad --gui=\"../scratch-gui\""
  },
  "devDependencies": {
    "eslint-config-scratch": "^7.0.0"
  }
}
